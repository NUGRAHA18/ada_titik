# Dependensi
node_modules/

# Environment Variables — JANGAN commit file .env asli!
.env
.env.*
!.env.example

# Folder upload foto (data lokal, masuk volume Docker di production)
uploads/

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS generated
.DS_Store
Thumbs.db

# SSL certificates (jangan commit ke git!)
nginx/ssl/*.pem
nginx/ssl/*.crt
nginx/ssl/*.key

# Production env (credentials nyata)
.env.production

# Claude Code (konfigurasi lokal)
.claude/

DEPLOY.md